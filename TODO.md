TODO List

1. Project Setup
   Scaffold the project structure for the browser extension.
   Ensure compatibility with major browsers (e.g., Chrome, Firefox).
   Set up build tools and configurations (e.g., Webpack, Vite, or Rollup).
   Configure manifest file (e.g., manifest.json) with required permissions.
2. Integrate Faceit API
   Establish a connection with the Faceit API.
   Create reusable functions or services for API calls.
   Document available endpoints for future reference.
3. Faceit API Testing
   Authenticate a user using the Faceit API.
   Retrieve and filter match data for specific use cases.
   Implement unit tests to verify API responses and ensure data integrity.
4. Build Extension UI
   Design and implement the extension’s user interface using DOM manipulation.
   Ensure responsiveness and a clean layout.
   Add interactivity for user inputs and API actions.
   Test UI functionality across different screen sizes and resolutions.
